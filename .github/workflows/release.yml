name: release

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+\\+[0-9]+"

jobs:
  release:
    uses: ./.github/workflows/docker_build.yml
    with:
      nightly: false
    permissions:
      packages: write
      contents: read
